import { Button, Layout } from 'antd';

const Index = () => {
  return (
    <Layout style={{ minHeight: '100vh' }}>
      <Button
        type="primary"
        style={{ margin: '20px' }}
      >
        Open Server
      </Button>
    </Layout>
  );
};

export default Index;
