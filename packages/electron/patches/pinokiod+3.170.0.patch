diff --git a/node_modules/pinokiod/kernel/index.js b/node_modules/pinokiod/kernel/index.js
index 0050965..3733fe2 100644
--- a/node_modules/pinokiod/kernel/index.js
+++ b/node_modules/pinokiod/kernel/index.js
@@ -804,6 +804,12 @@ class Kernel {
 
     let home = this.store.get("home") || process.env.PINOKIO_HOME
     
+    // Set default home directory if not configured
+    if (!home) {
+      home = path.resolve(os.homedir(), "pinokio")
+      this.store.set("home", home)
+    }
+
     // reset shells if they exist
     if (this.shell) {
       this.shell.reset()
